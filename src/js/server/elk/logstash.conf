input {
  beats {
    port => 5044
  }
}

filter {
  json {
    source => "message"
    target => "parsed"
  }
}

output {
  elasticsearch {
    hosts => ["http://host.docker.internal:5003"]
    index => "logs-%{+YYYY.MM.dd}"
  }
}
