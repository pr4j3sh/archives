# redis

- install

```bash
pip install redis
```

```bash
pip install redis[hiredis]
```

> `hiredis` provides faster performance

## usage

```bash
python main.py
```

> change `HOST`, `PORT` in `config.py`, if necessary
