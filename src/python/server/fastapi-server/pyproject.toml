[project]
name = "fastapi-server"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
authors = [
    { name = "prajesh", email = "prajesh.eleven118@gmail.com" }
]
requires-python = ">=3.14"
dependencies = []

[project.scripts]
fastapi-server = "fastapi_server:main"

[build-system]
requires = ["uv_build>=0.12.12,<0.13.0"]
build-backend = "uv_build"
